# Base API url (no trailing slash)
API_BASE=https://<API>

# Auth credentials (EXAMPLE ONLY - do not put real password here)
API_USERNAME=dev
API_PASSWORD=change_me

# Business config
AUTODROME_ID=1

# Serial config
SERIAL_PORT=/dev/ttyS0
BAUDRATE=115200

# GPIO pins (BCM numbering)
POINTER_BUTTON=4
CANCEL_BUTTON=17

# Token refresh window (seconds)
REFRESH_SAFETY_WINDOW_SEC=60

# Pointer press: how long to wait for next KSXT message (seconds)
KSXT_READ_TIMEOUT_SEC=2.0
